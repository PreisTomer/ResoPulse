{
  "name": "@resopulse/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types for ResoPulse — wire-level payloads consumed by both the web app and the API.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
